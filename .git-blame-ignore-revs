# Bulk reformat commits ignored by `git blame` (enabled with
# `git config blame.ignoreRevsFile .git-blame-ignore-revs`).

# Apply Prettier formatting across codebase
37203e9299a4c7129d0003da37ffe1c140a7b19c
